/*
 *  For the function which makes the thread (Jumbotron) a chat room
 *  This will be changed to live chatting service (synchronized).
 *  Soo Young Choi. 2017/09/19
 */

$(function() {
    $('button').click(function() {
    	var str;
    	switch(this.id) {
			case 'sendMsg_1': {
    			str = $('#inputMsg_1').val();
    			if(str == "") break;
        		$('#myClass').append('<div class="media">')
        				  .append('<div class="media-left">')
        				  .append('<img src="./image/img_avatar1.png" class="media-object" style="width:60px">')
        				  .append('</div>')
        				  .append('<div class="media-body">')
        				  .append('<h4 class="media-heading">(174) 최수영</h4>')
        				  .append('<p>').append(str).append('(17.09.18)</p>')
        				  .append('</div></div>');
        		break;
    		}
    		case 'sendMsg_2' : {
    			str = $('#inputMsg_2').val();
    			if(str == "") break;
    			$('#allClass').append('<p>(174) 최수영 : ' + str +' (17.09.18)</p>');
    			break;
    		}
    	}
    });
});